# ======================================================
# Spring Boot — application-prod.properties
# Path: /home/deploy/prava-online/application-prod.properties
# ======================================================

# ─── Server ───
server.address=0.0.0.0
server.port=8080

# ─── Swagger / SpringDoc ───
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.use-root-path=true
springdoc.swagger-ui.servers[0].url=https://pravaonline.uz
springdoc.swagger-ui.servers[0].description=Production Server

# ─── Nginx proxy ───
# Spring Boot proxy headerlarni qabul qilsin
server.forward-headers-strategy=framework
server.tomcat.remoteip.remote-ip-header=X-Forwarded-For
server.tomcat.remoteip.protocol-header=X-Forwarded-Proto

# ─── File Storage (lokal) ────────────────────────────────────────────────────
# UPLOAD_DIR: fayllar saqlash absolyut yo'li (server restart qilinganda ham ishlaydi)
app.storage.type=local
app.storage.local.upload-dir=/home/deploy/prava-online/uploads
# BASE_URL: 302 redirect uchun absolyut URL (installer download uchun)
app.storage.local.base-url=https://pravaonline.uz

# ─── Logging ─────────────────────────────────────────────────────────────────
logging.file.name=/home/deploy/prava-online/logs/application.log
logging.level.root=WARN
logging.level.uz.pravaonline=INFO
logging.logback.rollingpolicy.max-file-size=50MB
logging.logback.rollingpolicy.max-history=30

# ─── Multipart upload limit ───────────────────────────────────────────────────
# Installer fayllar uchun 500 MB
spring.servlet.multipart.max-file-size=550MB
spring.servlet.multipart.max-request-size=550MB
